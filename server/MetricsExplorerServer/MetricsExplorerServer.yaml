# server config

metrics_explorer:
  salt: "abcd"  # salt for sha256
  mysql:
    dsn: "root:root123@tcp(127.0.0.1:3306)/metrics_explorer?charset=utf8mb4&parseTime=True&loc=Local"
  http:
    port: 8099
    path: "/metrics_explorer/"
  admin:
    name: admin
    passwd: "admin"

